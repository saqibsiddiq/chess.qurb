# chesy

A local-first chess game review app. Load a PGN, and it walks through
every move with a real Stockfish evaluation and a Chess.com-style
classification (Brilliant, Great, Best, Excellent, Good, Book,
Inaccuracy, Mistake, Miss, Blunder),no account, no server, no usage
limits.

Chesy's own classification and explanation logic are documented
approximations of Chess.com's Game Review, not a reverse-engineered
copy of their (proprietary, undocumented) algorithm see
[`ml/specs/review_contract.md`](ml/specs/review_contract.md) for the
exact reasoning behind each class.

## Status

The classifier, board, move list, and progressive review UI are
working. Move explanations are currently generated by rule-based
templates, not the small language model described as the project's
long-term goal that model doesn't exist yet (see
[`ml/`](ml) for the in-progress training pipeline).

## Running it

Requires [Stockfish](https://stockfishchess.org/download/) on your
system `$PATH` unless you're using a build with it bundled (see
[`app/src-tauri/binaries/README.md`](app/src-tauri/binaries/README.md)).

```bash
cd app
npm install
npm run tauri dev
```

## Building a release

```bash
cd app
cp "$(which stockfish)" src-tauri/binaries/stockfish
chmod +x src-tauri/binaries/stockfish
npm run tauri build
```

## Stack

Tauri (Rust) + React/TypeScript, real Stockfish over UCI, no telemetry,
no network calls at runtime.

## License

See [LICENSE](LICENSE). Chesy bundles the
[Stockfish](https://github.com/official-stockfish/Stockfish) chess
engine, licensed under the GNU GPLv3 — see
[`app/src-tauri/binaries/README.md`](app/src-tauri/binaries/README.md)
for its own licensing and source.
